#!/usr/bin/make -f

QT_SELECT=5

build:
	make all

clean:
	make clean

binary: 
	make install DESTDIR=debian/kylin-language-pack-bo-cn-base
	make install DESTDIR=debian/kylin-language-pack-bo-cn-extra
	dh_makeshlibs
	dh_gencontrol
	dh_builddeb
